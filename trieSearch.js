// freecodecamp

// ordered search tree commonly used to hold strings
// associative arrays. dynamic datasets 
//    the keys are string.
// nodes not associated with actual values.
// the path to a node represents a specific key.

// following that path through all these nodes will create code.
    // that path is the key we stored.

class Person {
    constructor() {
        this.keys = new Map();
        this.engine = null;
    }
    press() {
        this.engine = true;
    }
    release() {
        return this.engine;
    }
    brake() {
        this.engine = false;
    }
}

class Trie {
    constructor() {
        this.khalid = new Person();
    }
    add(rap) {
        console.log(rap);
        if(mumble) {
            // console.log(Object.keys(this.khalid.keys));
            if(Object.keys(khalid.keys).includes([rap[0]]) {
            }
        } 
    }
}